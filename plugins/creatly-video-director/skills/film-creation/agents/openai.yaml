interface:
  display_name: Film Creation
  short_description: 在创作画布完成剧本、分镜、画面、视频与预览创作
  default_prompt: 使用 $film-creation 在创作画布创建一部完整剧情视频。
dependencies:
  tools:
    - type: mcp
      value: yuanji
      description: 当前发行版本配置的标准创作 MCP Server
